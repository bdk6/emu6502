//////////////////////////////////////////////////////////////////////////////
/// @file hex_loader.h
/// @brief Header for hex_loader.c to load intel hex file records type 00, 01
/// @copyright 2025 William R Cooke
//////////////////////////////////////////////////////////////////////////////

#ifndef _HEX_LOADER_H_
#define _HEX_LOADER_H_

#include <stdint.h>

#define MAX_RECORD_LENGTH 255

typedef enum
  {
    HEX_ERR_NONE    = 0,
    HEX_ERR_NOFILE  = (1<<0),
    HEX_ERR_EOF     = (1<<1),
    HEX_ERR_CS      = (1<<2),
    HEX_ERR_TYPE    = (1<<3),
    HEX_ERR_FORMAT  = (1<<4)
  } hex_error_t;

typedef struct
{
  hex_error_t error;
  int rec_type;
  int address;
  int bytes;
  uint8_t data[MAX_RECORD_LENGTH];
} hex_record_t;

int hex_open(char* path);
int hex_close(void);




#endif // _HEX_LOADER_H_
